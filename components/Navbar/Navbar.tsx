import * as React from 'react';
import './Navbar.scss';

const Navbar = () => {
  return;
  <nav>
    <div>
      <img src={images.logo} lat="logo" />
    </div>
  </nav>;
};

export default Navbar;
